//UI
const body = document.querySelector("body");

body.style["backgroundColor"] = "#F8B195";

const textDiv = document.getElementById("greeting");

textDiv.setAttribute("class", "typing")

// textDiv.appendChild(welcomeText);